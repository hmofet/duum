__pycache__/
*.pyc
*.pyo

# PyInstaller work + output (the .py distributions in dist/ ARE committed)
build/
exe/
*.manifest

# local scratch
*.wad
*.WAD
shot*.png
golden*.json
tools/duum_golden.json

# icon previews are for looking at, not for shipping
packaging/icons/preview-*.png

# the web port fetches MicroPython at build time (see ports/web/build.sh)
ports/web/micropython/
